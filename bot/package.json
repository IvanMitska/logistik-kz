{
  "name": "logistics-kaz-bot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Telegram bot + API for logistics.kaz — lead intake & order tracking",
  "scripts": {
    "dev": "node --env-file=.env --import tsx --watch src/index.ts",
    "start": "node --env-file=.env --import tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/static": "^8.0.4",
    "fastify": "^5.2.1",
    "grammy": "^1.34.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "~5.8.3"
  }
}
